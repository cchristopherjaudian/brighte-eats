export enum APP_SYNC_RESOURCE_IDS {
    LEADS_GRAPHQL_API = 'LeadsGraphqlApi',
    GET_LEADS_RESOLVER = 'GetLeadsResolver',
    GET_LEAD_RESOLVER = 'GetLeadResolver',
}

export enum GRAPHQL_DATASOURCES_ID {
    LEADS_CLUSTER_DATASOURCE = 'LeadsClusterDatasource',
}
